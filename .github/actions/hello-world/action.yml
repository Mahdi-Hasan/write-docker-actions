name: "custom-action"
description: "my first custom action"

inputs:
  firstGreeting:
    description:  "Who would you like to greet in the console"
    required: true
    default:  "Mahdi"
  secondGreeting:
    description:  "another person to greet"
    required: false
  pathOfGreating:
    description:  "path for greating"
    required: true
    default:  ".github/actions/hello-world/"
runs:
  using: "docker"
  image: "Dockerfile"
